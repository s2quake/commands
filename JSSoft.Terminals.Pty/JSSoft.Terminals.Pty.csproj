<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <PublishName>commands</PublishName>
    <DefineConstants>JSSOFT_TERMINALS</DefineConstants>
  </PropertyGroup>
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <DelaySign>false</DelaySign>
  </PropertyGroup>
  <Target Name="test" AfterTargets="CoreBuild">
    <Exec Command="gcc -dynamiclib -o '$(MSBuildThisFileDirectory)runtimes\osx-arm64\jspty.dylib' '$(MSBuildThisFileDirectory)runtimes/pty.c'" />
    <!-- <Exec Command="echo '$(TargetDir)jspty.dylib'" /> -->
  </Target>
  <!-- <Target Name="test" BeforeTargets="Build">
    <Message Text="Tes111t123" Importance="High" />
  </Target> -->
  <ItemGroup>
    <None Include="runtimes\osx-arm64\jspty.dylib">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>
</Project>
